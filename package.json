{
  "name": "is-html-tag",
  "version": "1.0.1",
  "description": "Check if the tag is a valid html tag",
  "main": "src/is-html-tag.js",
  "scripts": {
    "test": "nyc jasmine-node --verbose --captureExceptions test",
    "test:watch": "npm test && onchange lib/*.js test/* -- npm test",
    "coverage": "nyc report --reporter=text-lcov > coverage.json"
  },
  "author": "Alan Campora <alan.campora@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "jasmine-node": "^1.14.5",
    "nyc": "^10.2.0",
    "onchange": "^3.2.1"
  },
  "dependencies": {
    "html-tags": "^1.1.1"
  }
}
